package com.example.dndmanagerslim.components.character.npc

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import com.example.dndmanagerslim.data.NpcData

@Composable
fun NpcOverview(
    modifier: Modifier = Modifier,
    selectedNpc: NpcData,
) {
    // This is a placeholder for the NPC overview UI component.
    // You can implement the UI using Compose components like Text, Button, etc.
    // For example:
    /*
    Text(text = "NPC Overview")
    NpcTable(npcList = npcList, selectedNpc = selectedNpc)
    */
}